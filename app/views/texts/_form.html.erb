
<%= form_for @text do |f| %>

  <% if @text.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@text.errors.count, "error") %> prohibited
        this song from being saved:
      </h2>
      <ul>
        <% @text.errors.full_messages.each do |txt| %>
          <li><%= txt %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.label :artist %><br>
    <%= f.text_area :artist %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
